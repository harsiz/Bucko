client_version: 1

ui:
  default_next_label: "NEXT"
  typewriter_speed: 0.03       # seconds per character
  window_width: 900
  window_height: 650
  theme: "darkly"              # ttkbootstrap theme

mood:
  baseline:
    energy: 70
    patience: 80
    chaos: 40
    warmth: 75
  decay_rate: 1                # units per minute back toward baseline

discord_rpc:
  enabled: true
  app_id: "1234567890123456789"  # placeholder
